# Pygame prototype (archived)

An early desktop prototype built with pygame, before the project moved to the
web stack. It is **not** wired into the game and is kept here only for
reference. The maintained game lives in `../web_game/`.

Run (if you want to look at it):

```bash
pip install pygame
python david/prototype/main.py
```
